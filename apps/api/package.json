{
  "name": "@notification-cli/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "check": "tsc -p tsconfig.json",
    "test": "node --import tsx --test test/*.test.ts"
  },
  "dependencies": {
    "@azure/functions": "^4.7.0",
    "@notification-cli/core": "workspace:*"
  },
  "devDependencies": {
    "@azure/data-tables": "^13.3.2",
    "tsx": "^4.19.0"
  }
}
